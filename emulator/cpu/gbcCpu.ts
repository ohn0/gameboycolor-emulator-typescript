import { Logger } from "../../logger/logger";
import { RAM } from "../RAM/RAM";
import { CPU } from "./cpu";
import { InterruptHandler } from "./InterruptHandler";

export class GbcCpu extends CPU {
    // constructor(ram: RAM, logger: Logger, interruptHandler: InterruptHandler){
    //     super(ram, logger, interruptHandler, false);
    // }
}